server:
  port: 8080
  tomcat:
    threads:
      max: 400
      min-spare: 20
    accept-count: 200
    connection-timeout: 5000

spring:
  datasource:
    url: jdbc:mysql://10.1.131.131:30306/yortech_cdss?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect